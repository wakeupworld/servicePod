apiVersion: v1
kind: Service
metadata:
  name: event-monitoring
  namespace: eventhandler
  labels:
    app: configmap-event-handler
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: configmap-event-handler

