apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: configmap-event-handler
  namespace: eventhandler
rules:
- apiGroups: [""]
  resources: ["configmaps"]
  verbs: ["get", "patch"]

